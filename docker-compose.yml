version: '3.8'

services:
  cappo-backend:
    build: 
      context: .
      dockerfile: Dockerfile
    image: cappo-backend:latest
    container_name: cappo-backend-node
    restart: unless-stopped
    ports:
      - "127.0.0.1:8090:8000"
    environment:
      # Using the real DB
      - DATABASE_URL=postgresql+psycopg2://postgres:postgres@llwfyzhnft87bz6brddiax1z:5432/veklom
      - HOST=0.0.0.0
      - PORT=8000
    networks:
      - coolify

networks:
  coolify:
    external: True
