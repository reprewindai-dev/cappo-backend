name: Retired Activation integration helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Activation integration is committed to product code and verified by canonical CI."
