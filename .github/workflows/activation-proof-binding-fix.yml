name: Retired production proof key helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Stable production evidence-root enforcement is committed and verified by canonical CI."
