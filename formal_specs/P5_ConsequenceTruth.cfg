INIT Init
NEXT Next
INVARIANT NoOverclaim
INVARIANT NoIllegalTruthTransition
INVARIANT NoTruthWithoutAuthority
INVARIANT UnknownCannotSelfResolve
CONSTANT
    Operations = {op1, op2}
    Intents = {i1, i2}
    Consequences = {c1, c2}
    ProofTypes = {callback_return, outcome_uncertain, reconciliation_api_query, invalid_proof}
CHECK_DEADLOCK FALSE
